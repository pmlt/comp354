import static org.junit.Assert.*;

import org.junit.Test;

/*	Test that invalid VSF files are not accepted.
 *  A file with a timestep < 0.5 should have the timestep set to 0.5.
 *  A file with invalid vessels should have those vessels ignored. */
public class VSFReaderTest {

	@Test
	public void test() {
		
	}

}
